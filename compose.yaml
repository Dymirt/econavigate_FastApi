services:
  api:
    build: .
    restart: unless-stopped
    env_file: .env.local
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - eco-cache:/data/cache
    deploy:
      resources:
        limits:
          memory: 1G

volumes:
  eco-cache:
